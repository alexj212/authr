class Endpoints {
    LOGIN_API_ENDPOINT = "/login"
    REFRESH_API_ENDPOINT = "/refresh"
    REGISTER_API_ENDPOINT = "/register"
    LOGOFF_API_ENDPOINT = "/logout"
}

export default new Endpoints();
  
  